node_modules/
dist/
bin/native/
coverage/
*.vsix
*.log
.vscode-test/
.DS_Store
Thumbs.db

# Local scratch
artifacts/
.agents/
.semanticus/

# Working notes that land in this directory but belong to other projects. Two were committed by
# a `git add -A` before anyone noticed them; ignoring the pattern stops the next one.
*vision.md
